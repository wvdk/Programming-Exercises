#include <stddef.h>
#include <stdlib.h>
#include <stdio.h>

struct Node {
  int data;
  Node* link;
};

int main() {
  
  Node* myList;
  myList = NULL;

  Node* temp = (Node *)malloc(sizeof(Node));

  (*temp).data = 2;
  temp->data = 3;

  printf("%i", int(123));
  printf("testing");

  return 0;
}

